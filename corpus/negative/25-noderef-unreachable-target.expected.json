{
  "stage": "ingest",
  "family": "Malformed",
  "notes": "A local NodeRef whose target is reachable only through the (itself unreachable) NodeRef. The NodeRef's canonical form is its target's content hash, and the hash walk computes hashes only for root-reachable nodes, so the reference has no canonical form; ingest pass 2.7 rejects it. Found by the fuzzer's swap-reference operator on corpus 10 as a raw IllegalStateException escaping Hasher.finalize."
}
