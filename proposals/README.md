# Proposals

Design proposals for Strand language features that have been researched but not yet implemented. Each proposal is at the "ready to implement" level of detail: problem statement, recommended approach, implementation sketch, deferred concerns, scope estimate. The next session can advance any of them by reading the corresponding document, confirming the recommendation with the project lead, and executing.

Proposals here are **drafts**, not accepted decisions. They have not been promoted to ADR status. A future session that implements a proposal should:

1. Read the proposal and the related design documents (cited in each proposal's header).
2. Confirm or revise the recommendation with the project lead.
3. Implement against the proposal.
4. Either delete the proposal file (once implemented) or update it to note "Implemented in <commit/milestone>" and move to a `proposals/implemented/` subdirectory.
5. Update `open-questions.md` to mark the corresponding Q-NNN as Resolved.

## Current proposals

| File | Topic | Question | Drafted |
|------|-------|----------|---------|
| [`model-api-integration.md`](model-api-integration.md) | Dynamic-cost evaluation framework: Python harness driving Claude via Anthropic API through the verifier-feedback retry loop, with mocked-mode fixture replay for CI, multi-baseline support (Python+type-hints / Strand canonical / Strand Layer A density v4 in the initial slice), and per-run timestamped reports rolling up to `evaluation/dynamic-results.md`. Closes the dynamic-cost half of Q-021 / Q-034 by measuring tokens-per-successful-task across an agent's retry loop. Partially implemented as of 2026-05-25: the `strand-eval` Python framework, Anthropic + step-mode (Claude Code) backends, Python language adapter, 10 tasks, and a first end-to-end run (`evaluation/dynamic-results.md`) all ship; outstanding follow-ups are an Anthropic-backend run with fresh contexts (the first run's numbers are upper-bounded by prior-corpus exposure), prompt-caching measurement, multi-sample statistical aggregation, retry-loop-exercising tasks, and the remaining four Q-021 baselines. | Q-021 (dynamic), Q-034 (dynamic) | 2026-05-25 |
| [`cross-store-federation.md`](cross-store-federation.md) | Layer 2 step 3: cross-store federation and hash-pinned composition. Two coordinated mechanisms: (1) a `NodeResolver` interface composed in a chain (`LocalHashStoreResolver`, `ChainedResolver`, `FileSystemResolver`, `CachingResolver`) plus an off-graph `NameRegistry` JSON file; NodeRef resolution falls through the resolver chain on cache miss with hash-integrity check. (2) A new node category **N-046 ModuleManifest** as a content-addressed grouping primitive — bundles exports with aggregate effect declarations; each export is `(target: Hash, declaredEffects: List<EffectCategory>, displayName: String)`; the verifier admits the manifest by checking declared effects match each export's closure exactly. ADR-003 amended 2026-05-28 to clarify its conclusion: it rejects name-based identity, import-path resolution, visibility, and version constraints — it does not reject content-addressed grouping primitives. The manifest reintroduces none of the rejected features (displayName is metadata; consumers reference exports by hash directly; every hash stays callable). Adds bundle-level effect declarations, signing surface for Q-006, distribution unit, and cognitive grouping for agents. Closes the "every program is monolithic" expressiveness ceiling identified as Tier 2 Item 1 of the roadmap. Network resolvers, signed manifest verification (Q-006), persistent caching, multi-version naming, async resolution, cross-store GC, and sealed exports explicitly deferred. **N-046 ModuleManifest (step 3b) landed 2026-05-28** (the verifier certifies each export's effect surface against `declaredEffects`); the **step-3a federation runtime landed 2026-05-29** (`translateNodeIds` + bound-node re-basing admission + Merkle integrity; verifier/interpreter threading; the `targetHash` external-hash NodeRef ingest form; corpus 76; and `strand run/verify --peer-store` — `strand run app.json --peer-store lib.json` → 42). Remaining for step 3a: VM/runtime/schema threading, `--peer-store` on `machine`/`group`, the `strand registry` subcommands + flags, and corpus 77/78. See the proposal's § 10. | Q-043 | 2026-05-28 |
| [`stdlib-future-builtins.md`](stdlib-future-builtins.md) | Tracking catalog of builtin capabilities absent from the current registry (~110 builtins across 18 namespaces after rounds 1–3). Grouped by readiness: Round 4 agreed slice (Float arithmetic + comparisons, Bool.Eq/Bytes.Eq, List reducers + structure ops, Path.*, DateTime.*, Markdown.Stringify) is mechanical via the `strand-add-builtin` skill; Round 5+ candidates (String formatting, Set.*, Map extensions, CSV/TSV, URL, compression) ship the same way; open-design items (Crypto.*, TLS/mTLS, WebSocket) need dedicated proposals; the milestone-deferred set tracks Hardware/Trust/Memory.MutableState/Filesystem.Watch/Process.Signal/Time.Schedule/HTML/SVG/PDF against their unblockers. Strand-distinctive agent-native capabilities (LLM.*, vectors and embeddings, agent-state primitives, model routing) are referenced out to a separate research proposal because they may shape stdlib and node-algebra requirements before the rest of this catalog ships. | n/a (catalog) | 2026-05-26 |

Twenty-five Wave-3+ implementation proposals have landed and live in `implemented/` (Q-048 in-language error recovery (N-047 Attempt), Q-030 effect handlers, Q-031 refinement-lattice capability matching, Q-032 state machines step 1, Q-033 state machines step 2, Q-034 LLM authoring layer step 1, Q-035 schema + invariant step 1, Q-036 reverse projection, Q-037 agent-native LLM capabilities Phase 1, Q-038 agent-native vector stores Phase 1, Q-039 foreign effect projections, Q-040 interpreter resource limits, Q-041 I/O builtin sandboxing, Q-042 credential isolation, Q-045 streaming and asynchronous I/O, Q-047 runtime schema enforcement (Layer 7 step 2, foundational slice), layer-4-step-2 real IO, layer-a-density v1–v4, plaintext + markdown blessed libraries, JSON blessed library, bytecode VM step 1, stdlib expansion round 2, nested RecursiveSelf depth). All four 2026-05-26 audit findings (Q-039 / Q-040 / Q-041 / Q-042) are now resolved. Three drafts remain in this directory: `model-api-integration.md` (Q-021 / Q-034 dynamic-cost evaluation, partially shipped), `cross-store-federation.md` (Q-043 Layer 2 step 3 + N-046 ModuleManifest — fully resolved 2026-05-29; retained here pending the move to `implemented/`), and the `stdlib-future-builtins.md` tracking catalog.

## Implemented proposals

Moved to `proposals/implemented/` once executed. Each retains its full text plus an "Implementation note" header recording any deviations and the link into the implementation.

| File | Topic | Question | Implemented |
|------|-------|----------|-------------|
| [`implemented/error-recovery.md`](implemented/error-recovery.md) | Q-048: in-language error recovery via **N-047 Attempt** — `Attempt(body)` typed by the verifier-synthesized structural sum `Ok(T) \| Err({kind: String, detail: String})`, consumed with ordinary Match machinery. Catchability is an `isCatchable` val on the sealed `InterpretError` hierarchy: only `IoFailure` and `SchemaInvariantViolation` are catchable; program defects, host-policy stops (`ResourceExhaustion`, `SandboxViolation`, capability/refinement denial), and infrastructure errors terminate regardless of any enclosing Attempt. `closureOf(attempt) = closureOf(body)` — failures are not effects, so the Q-044 harm bound is unchanged, and a catch does not reset Q-040 counters (retry loops are budget-bounded by construction). Both backends in one slice: interpreter `evalAttempt`; VM `ATTEMPT_PUSH`/`ATTEMPT_POP` attempt-marker unwind reusing `SUM_NEW`, which also closed the pre-existing VM IoFailure/SandboxViolation translation parity gap. Layer A `TRY` + `RES` sugar + `errPayloadT` prelude + reverse projection; canonical tag 47 specified in `design/canonical-encoding.md` with a byte-trace test; corpus 85–87 (Ok passthrough, Fs.Read fallback, retry-with-backoff), all VM-equivalent. Seven recorded deviations in the Implementation note; WHEN-from-TRY elaborator inference and the `BuiltinContractViolation` hygiene variant deferred. | Q-048 | 2026-06-11 |
| [`implemented/refinement-lattice-capability-matching.md`](implemented/refinement-lattice-capability-matching.md) | Layer 3 step 2: parameterized effect matching with wildcard capabilities | Q-031 | 2026-05-23 |
| [`implemented/effect-handlers.md`](implemented/effect-handlers.md) | Layer 3 step 3: no-continuation effect handlers (N-043 Handler) | Q-030 | 2026-05-23 |
| [`implemented/state-machines-runtime.md`](implemented/state-machines-runtime.md) | Layer 6 step 1: synchronous trace runtime for state machines (steps 2 and 3 still deferred) | Q-032 (step 1; Q-008 unchanged) | 2026-05-24 |
| [`implemented/schema-and-invariant.md`](implemented/schema-and-invariant.md) | Layer 7 step 1: N-032 Schema + N-033 Invariant nodes; pure-expression invariants on statically-known values; new `:schema` Gradle module; synthetic PositiveInt / NonEmptyList corpus | Q-035 | 2026-05-24 |
| [`implemented/state-machines-runtime-step-2.md`](implemented/state-machines-runtime-step-2.md) | Layer 6 step 2: async multi-machine actor runtime via Kotlin coroutines + Channel<Value> + select; tagged-Event sums; inter-machine wiring; multi-input supervisor pattern; recursive-list output; `strand group` CLI | Q-033 | 2026-05-24 |
| [`implemented/state-machines-runtime-step-3.md`](implemented/state-machines-runtime-step-3.md) | Layer 6 step 3: all six slices — bounded-queue overflow policies (Q-015), in-band supervision via E-030/E-031 + host-driven spawn/terminate, content-addressed snapshot/replay, runtime metrics + `--metrics` CLI, implicit Send/Receive verifier enforcement via WellKnownEffect registry, multi-producer fan-in + broadcast fan-out via ConsumerMode + StreamBus | Q-008 (part), Q-015 | 2026-05-24 |
| [`implemented/llm-authoring-layer.md`](implemented/llm-authoring-layer.md) | Q-034 step 1: four-layer LLM emission stack — full-coverage Layer A grammar + parser + dag-json emitter, Layer B GBNF constraint grammar via `strand grammar`, and an Elaborator that now ships **eleven** Layer C inference cases (the original four — Lambda effects, effectInstances defaulting, Application typeArguments, Lambda paramType — plus seven added during the v4 work: recursion-slot paramType, FunctionType synthesis, SumTypeCase caseType from payload, and four expanded compact-LAM-param paths via call sites + StateMachine transitionFn signature + Match scrutinee + ProductFieldValue + ProductFieldGet contexts). The JSON→Layer A reverse-translation surface (`strand translate` plus `LayerATranslator`/`LayerARenderer`) and the `--elaborate` flag on `strand author` were retired in the 2026-05-25 cleanup pass — elaboration is always-on and emission is the only direction. Layer A density v1-v4 expands this work and is the canonical reference for the seven added inference cases at [`implemented/layer-a-density.md`](implemented/layer-a-density.md). | Q-034 step 1 | 2026-05-24 |
| [`implemented/bytecode-vm-step-1.md`](implemented/bytecode-vm-step-1.md) | Q-017 step 1: Kotlin reference bytecode VM. `:bytecode` + `:vm` Gradle modules, 28-opcode stack-based instruction set, uniform-boxed value representation with VmClosure/VmFixpoint/VmForeign callables carrying effects, capability stack + active handler list, EQ/SUM_CASE_IS/SUM_PAYLOAD/THROW_NO_MATCH helpers, JUMP backpatching for Match dispatch. `Vm.applyClosure` + `Vm.evaluate` public APIs enable runtime/schema dispatch through the VM. **52 of 58 corpus programs pass `interpreter == VM` equivalence** across VmEquivalenceTest (37 Layers 1/3/4/5 programs), VmMachineEquivalenceTest (7 single-input state machines), VmSchemaEquivalenceTest (7 schemas). Multi-input/async actor-loop integration (corpus 47/48/49) is a follow-up; the value-level mechanism is in place. Step 2 (Rust port per ADR-008) is the next milestone. | Q-017 step 1 | 2026-05-24 |
| [`implemented/plaintext-and-markdown-libraries.md`](implemented/plaintext-and-markdown-libraries.md) | Q-026 blessed-library expansion: PlainTextDocument (String + NonEmptyText variant using `Bool.Not(String.Eq(s, ""))`) and MarkdownDocument (four-variant MarkdownBlock sum + recursive-list document + NonEmptyMarkdown invariant using `Match(Cons→true, Nil→false)`). Six new corpus programs (58-63) covered by CorpusSchemaTest + VmSchemaEquivalenceTest; four ship in Layer A form. HTML5 + SVG + PDF deferred (nested-μ blocker for HTML/SVG; binary format for PDF). | Q-026 (part) | 2026-05-24 |
| [`implemented/json-blessed-library.md`](implemented/json-blessed-library.md) | Layer 7 step 1.5: first blessed output library — `JsonValue` (flat primitives sum) + `UniqueKeyJsonObject` Schema with `Fixpoint`+`Match` `unique_keys` invariant; three new corpus programs; `Bool.And`/`Bool.Or`/`String.Eq` builtins | Q-026 (first slice) | 2026-05-24 |
| [`implemented/layer-a-density.md`](implemented/layer-a-density.md) | Layer A density v1 through v4 (10 slices + 7 Elaborator inference extensions): implicit prelude, inline literals, auto-VarRef, IF, compact LAM params, inline literal patterns, anonymous nodes + @last, inline ProductFieldValue list, WHEN, plus Slice 10 nested expressions (`(CODE args...)` inline inside `[...]` lists and at REFERENCE / NULLABLE_REF slots, gated by a `CodeSchema.producesValue` flag) and a seven-case extension to the Elaborator (recursion-slot paramType, FunctionType synthesis, SumTypeCase caseType from payload, compact-LAM param inference via call sites + StateMachine transitionFn signature + Match scrutinee + ProductFieldValue + ProductFieldGet contexts). Cuts the three-task evaluation geomean from 2.28× Python+type-hints to **0.81×**. All-additive grammar extensions; verifier and canonical CBOR encoder unchanged. The same session also retired the JSON→Layer A reverse-translation surface and the `--elaborate` flag — elaboration is always-on, density-vN fixtures plus LayerADensityTest are the regression net. | Q-034 follow-up | 2026-05-25 |
| [`implemented/layer-a-reverse-projection.md`](implemented/layer-a-reverse-projection.md) | Q-036: canonical dag-json → Layer A density-v4 text. Restores the reverse direction of the Q-034 authoring stack so agents reading existing Strand programs see the same compact representation they emit. Two-stage `LayerATranslator` (canonical walk + 10 density-sugar projection passes + SAFE/UNSAFE/probe-and-fallback elaboration omission across the 11 Elaborator inference cases) + `LayerARenderer` (deterministic text emission) + `Authoring.projectFromDagJson` + `strand translate <file.json>` CLI subcommand. `LayerAReverseRoundTripTest` asserts `forward_compile(render(translate(canonical))) == canonical` byte-for-byte across all 64 corpus programs. Three deviations from the literal plan: `ElaborationOmission.kt` folded into the translator, `Lambda.effects` demoted SAFE→BORDERLINE (corpus 12/13/14 over-declare effects), new `FORCE_ALL_OPTIONALS = setOf("APP")` rule to short-circuit Application effectInstances defaulting on corpus 33-35. | Q-036 | 2026-05-25 |
| [`implemented/layer-4-step-2-real-io.md`](implemented/layer-4-step-2-real-io.md) | Layer 4 step 2: real IO builtins across Filesystem (`Fs.*`), Network sockets (`Net.*`), HTTP (`Http.Request`), Process (`Process.Spawn/Wait/EnvVar`), Time (real wall-clock + Sleep, injectable Clock), plus a String + Bytes stdlib (Length/Substring/IndexOf/Replace/Split/Join/Trim/ParseInt/ParseFloat/etc.) and `Json.Parse` + `Markdown.Parse` to complete the existing blessed libraries. 28 new builtins; new `Value.Resource(id, kind)` opaque-handle variant + thread-safe `ResourceTable`; new `IoFailure` runtime exception translated to `InterpretError.IoFailure`. Plus the Elaborator's auto-Outer-PRD synthesis (task #22) and two new corpus exemplars (64-65 — canonical Option pattern with ParseInt). Three deviations from the literal plan: new `Fs.*` / `Net.*` namespaces rather than overloading legacy `Filesystem.*` / `Network.*` stubs to preserve seed-corpus hashes; `Time.Now` reads from injectable `Builtins.clock` (default SystemClock, tests install FixedClock); `Test.EffectfulNoOp` test helper added so two InterpreterTest cases stop abusing Filesystem.Write semantically. WASM sandboxing (Q-006, Milestone 2.4) remains deferred. | Layer 4 step 2 | 2026-05-26 |
| [`implemented/stdlib-expansion-round-2.md`](implemented/stdlib-expansion-round-2.md) | Stdlib expansion round 2: pure-utility Math.* (Abs/Sign/Min/Max/Mod/Floor/Ceil/Round/Sqrt/Pow/Log/Exp/Sin/Cos/Tan) + Float.FromInt/Int.FromFloatTrunc coercions, Hash.* (Blake3/Sha256/Md5 with official test-vector coverage) reusing the project's existing Blake3 primitive, List.* primitives that don't need lambdas (Empty/IsEmpty/Length/Reverse/Take/Drop/Concat/Nth), Json.Stringify, Bytes hex codecs (FormatHex/ParseHex), Random.* (Int/Float/Bytes) via injectable SecureRandom mirroring the Clock pattern. Slice 2 adds the higher-order interpreter callback infrastructure (Builtins.ApplyFn / FnH / lookupHigherOrder, Interpreter.applyValueToArgs) and the six higher-order List ops (Map/Filter/Fold/Find/Any/All) on top. ~39 new registry entries total, new corpus 66 (JsonValueFull), Json.Parse / Json.Stringify rewritten for arrays + objects. | n/a | 2026-05-26 |
| [`implemented/nested-recursive-self-depth.md`](implemented/nested-recursive-self-depth.md) | N-042 RecursiveSelf gains an optional `depth: Int = 0` field — a de Bruijn index against the recursive-binder stack. Default 0 preserves all existing program hashes; depth > 0 resolves to the N-th outer enclosing RecursiveType. Verifier + canonical encoder updated; two new VerifierTest cases. The field works as a type-algebra primitive but a value-construction limitation forced the JsonValueFull extension to use spliced variants (corpus 66) rather than the nested-μ shape originally planned — the depth field stays as foundational infrastructure for future use (polymorphic recursive types, etc.). | n/a (foundational) | 2026-05-26 |
| [`implemented/agent-native-capabilities.md`](implemented/agent-native-capabilities.md) | Q-037 Phase 1: per-provider LLM ForeignNodes for Anthropic / OpenAI / Gemini under operation-shaped effect categories E-035 LLM.Generate{provider, model} and E-036 LLM.Embed{provider, model}. 23 files / +3340 lines / 34 new tests. New `CredentialProvider.kt` (unified with Q-038), three per-provider Kotlin objects with injectable `LlmHttpClient`, `LlmProviders.kt` shared shapes, `JsonSchemaProjection.kt` (irreducible TypeExpr → JSON Schema), `VerifyError.ToolParamTypeUnsupported`, six `strand-builtin:` registry entries with tool-loop, `llm_conversation` ResourceTable kind, prelude entries, system-prompt section, corpus 67 verify-only state-machine demonstrator. Five recorded deviations (partial verifier walk for ToolParamTypeUnsupported, bytesT prelude placeholders, Anthropic embeddings deferring to Voyage, single global llmHttpClient, merge-time CredentialProvider unification). Phase 2 remains. | Q-037 (Phase 1) | 2026-05-26 |
| [`implemented/agent-native-vector-stores.md`](implemented/agent-native-vector-stores.md) | Q-038 Phase 1: per-provider vector-store ForeignNodes for Pinecone + Chroma under operation-shaped effect categories E-037 Vector.Read{provider, store} and E-038 Vector.Write{provider, store}. Read/Write split mirrors Filesystem.Read/Filesystem.Write. Twelve builtins total. 24 new tests. New `PineconeProvider.kt` / `ChromaProvider.kt` with injectable `VectorHttpTransport`, `VectorTypes.kt` shared shapes, `pinecone_index` / `chroma_collection` ResourceTable kinds, prelude entries, system-prompt Vector stores section, corpus 68 (renamed from 67 at merge) verify-only Pinecone exemplar. Four recorded deviations (bytesT opaque-handle convention, verify-only corpus, merge-time CredentialProvider unification, Manhattan-metric wire-rejection). Phases 2 (pgvector + FAISS) and 3 (Weaviate + Qdrant + RAG demo) remain. | Q-038 (Phase 1) | 2026-05-26 |
| [`implemented/foreign-effect-projections.md`](implemented/foreign-effect-projections.md) | Q-039: closes the implementation-level confused-deputy gap from the 2026-05-26 security audit ([`security-index.md`](../security-index.md) § Finding 1). Adds an optional `effectProjections` field to `ForeignNode` and `FunctionType` that pins each declared effect's parameter values to specific function arguments (`ArgRef(i)`) or binding-controlled literal nodes (`LiteralNode(t)`). The verifier requires authored EffectDecls at projected call sites to match the projection structurally (NodeId equality for ArgRef sources, canonical-form equality for LiteralNode sources); the interpreter synthesizes the capability-check parameter values from the projection plus the actual evaluated arguments, eliminating drift by construction. Six prelude entries migrated (`fsRead`, `fsWrite`, `fsAppend`, `fsExists`, `fsDelete`, `netConnect`); per-provider LLM/Vector bindings, `Crypto.RandomBytes`, `Http.Request`, `Process.Spawn`, and `Crypto.Sign/Encrypt/Decrypt` deferred to follow-up slices (LLM/Vector need binding-signature redesign to surface `model`/`store` as positional args; Crypto.RandomBytes needs an EffectCategory parameter refresh; Http.Request is paired with Q-041). Corpus 70 (happy path) + 73 (drift-rejected) + 8 verifier tests + 4 interpreter tests. Three recorded deviations (per-provider LLM/Vector deferred, Crypto.RandomBytes deferred, corpus slots shifted from 69/70 to 70/73 because slot 69 was already taken by the N-045 demonstrator and slots 71/72 were reserved by the in-flight Q-040 corpus). No new node category. | Q-039 | 2026-05-27 |
| [`implemented/interpreter-resource-limits.md`](implemented/interpreter-resource-limits.md) | Q-040: closes the DoS gap from the 2026-05-26 security audit ([`security-index.md`](../security-index.md) § Finding 2). Adds `core/EvaluationLimits.kt` (eight-field policy + DEFAULTS / PERMISSIVE companions, ExhaustionKind enum), threads counters and limits through `Interpreter` and `Vm` and `StateMachineRuntime`, surfaces breaches as `InterpretError.ResourceExhaustion(at: NodeId?, kind, current, limit)`. JSON ingest gains depth / byte / node-count caps; the tree-walking interpreter and the bytecode VM share a uniform contract; the state-machine runtimes inherit per-evaluation budgets. New CLI flags `--max-steps`, `--max-stack-depth`, `--max-allocated-values`, `--wall-clock-ms`, `--max-json-depth`, `--max-node-count`, `--max-ingest-bytes` on `run`, `machine`, `group`. `HaltReason` promoted from enum to sealed class with `ResourceExhaustion(kind, atEventIndex)` variant. 25 new tests across 5 modules; 2 new corpus programs (71, 72). Five deviations recorded: corpus 72 depth lowered from 800+ to 300 because the canonical Hasher is itself recursive and overflows the JVM stack before evaluation begins; scenario 6 and corpus 71 tests accept either WallClock/Steps or StackDepth because tree-walker depth grows linearly with productive recursion; `Value.UnitV` singleton is not counted; `EvalCounters` promoted from internal to public so `:runtime` can share counter state across per-event applyCallable calls. No new node category, ADR, or effect-category identifier required. | Q-040 | 2026-05-27 |
| [`implemented/credential-isolation.md`](implemented/credential-isolation.md) | Q-042: closes the credential-leakage gap from the 2026-05-26 security audit ([`security-index.md`](../security-index.md) § Finding 4). Adds `interpreter/Credential.kt` (opaque wrapper with redacted `toString`, explicit `reveal()`, reference-identity equality), `interpreter/CredentialScrubber.kt` (process-global value→placeholder registry with `< 8` char / blank-value skip rule), changes `CredentialProvider.resolve` and `apiKey` return type from `String?` to `Credential?`, threads scrubbing through `IoFailure` construction (dual `detail` / `unscrubbedDetail` storage), and grafts `errorVerbosity: ErrorVerbosity` onto Q-040's `EvaluationLimits` with three variants (`Redacted` default, `Full`, `RedactedWithKindOnly`). `Interpreter.translateIoFailure` centralises the verbosity-aware IoFailure translation; all four catch sites in `Interpreter.kt` plus the three `ForeignDispatcher.dispatch` calls route through it. CLI flag `--error-verbosity {redacted|full|kind-only}` on `run`, `machine`, `group`. Seven `.reveal()` call sites in production code total — auditable via `git grep -n '\.reveal()'`. 34 new tests; full `gradle test` green with 895 tests passing. Six recorded deviations (dispatcher path also catches IoFailure for defensive coverage; same-module test construction via internal constructor; Pinecone/Chroma handle-shape change for cached credential; `InterpretError.IoFailure` retains single-`detail` shape with dual storage on the runtime exception; `unscrubedDetail → unscrubbedDetail` spelling correction; no new corpus programs). No new node category, ADR, or effect-category identifier required — credential handling is runtime policy. | Q-042 | 2026-05-27 |
| [`implemented/runtime-schema-enforcement.md`](implemented/runtime-schema-enforcement.md) | Q-047 (Layer 7 step 2, foundational slice): runtime schema enforcement. Lifts Schema/Invariant evaluation from verify-time-on-statically-known-values to runtime-on-dynamic-values, resolving step 1's non-enforcing `SchemaInvariantDeferred`. The interpreter takes a `schemaObligations: Map<NodeId, TypeExpr.SchemaType>` (built from `VerifyResult.Ok.nodeTypes`) and, at the `eval` exit for an obligation-site NodeId, evaluates each pure-expression invariant against the produced value (empty capabilities, run's `EvaluationLimits`, `inInvariant` re-entrancy guard), raising the new recoverable `InterpretError.SchemaInvariantViolation` on `false`. Verify-time still rejects statically-provable violations early. No new node category, ADR, or effect-category identifier; no hash impact. Corpus 82 (dynamic pass → IntV(2)) + 83 (dynamic violation); `CorpusRuntimeSchemaTest` (5 cases, incl. an interpreter-with-obligations==VM pass-case cross-check) and corpus 82 in `VmEquivalenceTest`; full suite 1157 tests green. Deferred under the same identifier: ForeignNode-backed checkers (trust model via Q-006), the HTML5/SVG/PDF blessed-library set (HTML5/SVG blocked on nested-recursive-self), bytecode-VM enforcement (the VM erases schemas pre-bytecode, Q-017 — interpreter/VM diverge on runtime-violating programs until a `CHECK_SCHEMA` lowering lands), and schema-typed state-machine validation. | Q-047 | 2026-06-03 |
| [`implemented/streaming-async-io.md`](implemented/streaming-async-io.md) | Q-045 (Tier 2 part 3): streaming and asynchronous I/O. A uniform synchronous streaming-handle contract on the existing `ResourceTable`/`Net.Receive` pattern. Six builtins: `{Anthropic.Messages,OpenAI.Chat,Gemini}.CreateStream` (open → `Int` handle, declaring E-035 `LLM.Generate`), `LLM.Stream.Receive` / `Net.Stream.Receive` (`(handle, maxBytes) -> Option<Bytes>`, `Some(chunk)`/`None`-at-EOF, declaring E-004 `Network.Receive`), `LLM.Stream.Close` (idempotent; `Net.Close` serves the socket close). New `LlmHttpClient.postStream` transport seam (default buffering adapter over `post()` keeps existing mocks working; `DefaultLlmHttpClient` does true incremental streaming), per-provider `openStream`, `ResourceTable.KIND_LLM_STREAM` + `LlmStreamHolder`, `EvaluationLimits.streamReceiveTimeoutMillis` mirrored to a `Builtins` `@Volatile` singleton installed as socket `SO_TIMEOUT` / HTTP read timeout at open, `--stream-receive-timeout-ms` CLI flag, a Streaming I/O system-prompt section, verify-only corpus 81, and `StreamingReceiveTest` (10 cases). No node encoding changes — resources are runtime-only. Seven recorded deviations, chiefly: none of the streaming builtins are in the implicit prelude (receive drains are `Option<Bytes>`-returning, opens are agent-typed `GenerateRequest` — both established exception classes); no separate sandbox net check at LLM stream open (mirrors the blocking LLM path); corpus 81 verify-only with behavioral coverage in `StreamingReceiveTest`. § 8 deferred: actor-runtime bridge, stateful SSE decoder + normalized `StreamEvent`, process/file streaming, streaming send, TLS/WebSocket. | Q-045 | 2026-06-02 |
| [`implemented/actor-runtime-stream-bridge.md`](implemented/actor-runtime-stream-bridge.md) | Q-046: actor-runtime bridge for IO-backed external event streams. Resolves the bridge deferred in Q-045 § 8 — a state machine consumes a live socket/LLM stream as ordinary events. An additive optional `source: NodeId?` edge on `EventStream` (N-028) binds an `External` stream to its IO opener (`*.CreateStream` / `Net.Connect`); a host-side feeder launched at `runGroup` drains the Q-045 handle on `Dispatchers.IO` and pushes chunks through the existing `OverflowDispatcher`, inheriting `EventRecorder` replay. The opener's transport effects ∪ `Network.Receive` are gated against `group.capabilities` at group start (new `ExternalStreamSourceEffectUncovered`), keeping the Q-044 harm bound sound. Core slice: raw-`Bytes`, `BlockProducer`-only, per-instance replay. Five new verifier variants; no new node category, ADR, or effect-category identifier; all pre-Q-046 hashes preserved. Verify-only corpus 84 + 8-case `BridgedStreamTest`. Decoded events, group-level replay, streaming send, Layer A `source` grammar deferred. | Q-046 | 2026-06-03 |
| [`implemented/io-builtin-sandboxing.md`](implemented/io-builtin-sandboxing.md) | Q-041: closes the SSRF / path-traversal gap from the 2026-05-26 security audit ([`security-index.md`](../security-index.md) § Finding 3). Adds `interpreter/SandboxPolicy.kt` (host-configured `SandboxPolicy(fs, net)` ADT with `OPEN_DEFAULT` / `SECURE_DEFAULT` companions, `FsSandbox.resolve` for workspace-containment + symlink rejection via canonical path, `NetSandbox.checkConnect` for hostname blocklist + allowlist + multi-A IP-range check + DNS pin-at-check returning the resolved `InetAddress`), `interpreter/ResourceTable.kt` adds `SandboxViolation(kind, detail)` runtime exception + `SandboxViolationKind` enum (8 variants), `InterpretError.SandboxViolation(at, kind, detail)` verifier-translated variant. `Builtins.sandboxPolicy: SandboxPolicy` + `Builtins.nameResolver: NameResolver` ship as `@Volatile` singletons. Every `Fs.*` builtin runs paths through `FsSandbox.resolve`; `Net.Connect` runs `(host, port)` through `NetSandbox.checkConnect` and passes the resolved IP to `Socket(InetAddress, port)`; `Http.Request` redesigned to the seven-arg signature `(host, port, scheme, path, method, headers, body)` so Q-039's projection vocabulary binds host/port positionally; legacy `Http.RequestFromUrl(method, url, body)` preserved as wrapper that parses and dispatches through the same sandbox check. Scheme validated to http/https — `file://` raises `HttpSchemeRejected`. CLI flags `--workspace-root`, `--allow-fs-escape`, `--allow-host` (repeatable), `--allow-net-internal` on `run`, `machine`, `group`; CLI default is `SECURE_DEFAULT` (workspace = cwd, default-deny network with full blocked-range list). 25 new `SandboxPolicyTest` cases (one Windows-skipped for symlink fixture) covering all 12 proposal § 7 scenarios plus auxiliary policy primitives, 2 new `CorpusSandboxTest` cases driving corpus 74 (fs-write-escape-rejected) + 75 (http-metadata-rejected); 922 tests total, zero regressions across the 895 pre-Q-041 baseline. Five recorded deviations: singleton default is `OPEN_DEFAULT` while CLI default is `SECURE_DEFAULT` (preserves pre-Q-041 baseline); `httpReq` prelude entry redirected to legacy `Http.RequestFromUrl` (new seven-arg signature stays out of prelude because its response includes a recursive headers list); header representation is canonical Cons/Nil SumV chain over `{name, value}` ProductV; Windows symlink test skipped per `@DisabledOnOs(OS.WINDOWS)`; hostname blocklist factored into `NetPolicy.blockedHostnames` separate from `blockedRanges`. No new node category, ADR, or effect-category identifier required — sandboxing is runtime policy. | Q-041 | 2026-05-27 |

## Reading order recommendation

All Wave-3+ implementation proposals have landed and are retained in `implemented/` for reference. The state-machine series stacks: read `implemented/state-machines-runtime.md` (step 1, sync trace), then `state-machines-runtime-step-2.md` (step 2, async actor runtime), then `state-machines-runtime-step-3.md` (step 3, overflow + supervision + snapshot + metrics + fan-in/out). The Layer A authoring stack stacks similarly: `implemented/llm-authoring-layer.md` (Q-034 step 1, four-layer emission) → `implemented/layer-a-density.md` (10 density sugars, v4 inference extensions) → `implemented/layer-a-reverse-projection.md` (Q-036, canonical → density-v4). The only active draft is `model-api-integration.md`; partial implementation has shipped (`strand-eval` framework + first dynamic-cost run) and is tracked in [`../evaluation/dynamic-results.md`](../evaluation/dynamic-results.md).

## Sequencing notes

- Q-030 / Q-031 / Q-032-step-1 / Q-035-step-1 are independent of each other and of the recursive-types work.
- The state machines step 1 uses a fixed-arity `OutputBatch` workaround in place of a list-of-events return type. Step 2 (Q-033) switches to a recursive-list-based `(State, List<TaggedOutput>)` representation now that recursive types are landed; the OutputBatch path is preserved for single-stream machines so corpus programs 41–45 keep working unchanged.
- Identifier coordination: Q-030's effect-handlers work allocated `N-043`. Q-031's refinement-matching work allocated no new node category. Q-032's state-machines step 1 occupied the long-reserved `N-027`/`N-028`/`N-029` slots (already in the registry before the proposal). No new identifiers from step 1. Q-033 (state machines step 2) allocates no new node category — the runtime is the work, the algebra is unchanged. Q-035 (schema + invariant) occupied the long-reserved `N-032`/`N-033` slots.
- Corpus-program numbering: Q-031 used 33–35; Q-030 used 36–40; Q-032 step 1 used 41–45; Q-035 step 1 used 50–53. Q-033 will use 46–49 if implemented.
