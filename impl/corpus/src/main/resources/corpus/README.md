# Layer 1 Seed Corpus

Each JSON program is a small Strand graph using only N-001 through N-019 plus
N-034 (TypeAbstraction) and N-035 (ForallType). The verifier performs
explicit-instantiation type checking in the System F style: a polymorphic term
is a TypeAbstraction whose type is a ForallType, and every Application of a
polymorphic value supplies a positional `typeArguments` list whose length
equals the function's quantified type-parameter count. A monomorphic call
supplies an empty `typeArguments` list (or omits the field). Partial
instantiation is rejected. Type inference is not performed.

| File | Mode | Description |
|------|------|-------------|
| 01-int-literal.json | run | A bare IntLit `42`. Verifies to `Int`, evaluates to `42`. |
| 02-identity-applied.json | run | The identity `Λa. \x:a. x` applied to `42` with `typeArguments: [Int]`. |
| 03-let-identity.json | run | `let id = Λa. \x:a. x in id[Bool](true)`. |
| 04-k-combinator.json | run | The K combinator `Λa,b. \x:a y:b. x` applied to `(7, "ignored")` with `typeArguments: [Int, String]`. |
| 05-s-combinator-typed.json | verify-only | The S combinator wrapped as `Λa,b,c. \f g x. (f x)(g x)`. Inner Applications are monomorphic (their function types contain free TypeParameters bound by the enclosing TypeAbstraction, not by another Forall), so they carry `typeArguments: []`. Verifies the polymorphic type only. |
| 06-let-polymorphic.json | run | A single let-bound `id = Λa. \x:a. x` applied at two different types; each call carries its own `typeArguments`. |
| 07-higher-order.json | run | Higher-rank demonstration. A monomorphic function takes `f: forall a. a -> a` plus an `Int` and a `Bool`, applies `f` at `Bool` (for its side effect on type checking, then discarded) and at `Int`, and returns the `Int` result. The inner identity is genuinely polymorphic; its parameter type is a ForallType. This is the rank-2 example the System F refactor enables. |
| 08-product-type-decl.json | verify-only | A Lambda whose parameter is a `ProductType { a: Int, b: Bool }`; demonstrates type wiring without a product-value constructor (Layer 1 has none). |
| 09-sum-type-decl.json | verify-only | A Lambda whose parameter is a `SumType` (Option-like); demonstrates type wiring without a sum-value constructor. |
| 10-noderef-shared.json | run | Uses `NodeRef` to share a single IntLit between two Application positions; each call instantiates `id` at `Int`. |
| 11-higher-rank-apply.json | run | A minimal explicit higher-rank example: `apply : (forall a. a -> a) -> Int -> Int = \f:(forall a. a -> a). \n:Int. f[Int](n)` applied to a polymorphic identity and `7`. Evaluates to `7`. |
| 12-effect-declared-and-granted.json | run (under {Time.Now}) | A Lambda over `Int → Int` that declares `Time.Now`. The body returns its parameter; the effect is over-declared (not actually exercised). Demonstrates the minimum coherent capability check: the Application succeeds because the calling context grants the declared effect. |
| 13-capability-scope-narrow-then-call.json | run (under {Time.Now, Network.Connect}) | A `CapabilityScope` that narrows the surrounding context to `{Time.Now}` and then calls a Lambda that declares `Time.Now`. The body succeeds because the needed effect survives the narrowing. The other declared categories (`Network.Connect`) are filtered out by the scope. |
| 14-multi-effect-lambda.json | run (under {Time.Now, Network.Connect, Filesystem.Read}) | A Lambda declaring three effects, applied at `Int → Int`. The interpreter checks every declared effect against the context — all present, so the call proceeds. |
| 15-builtin-add.json | run | The first program that does real computation: a `ForeignNode` targeting the in-process `strand-builtin:Int.Add`. Pure (no declared effects); requires no capabilities. Evaluates to `5`. |
| 16-builtin-time-now-under-capability.json | run (under {Time.Now}) | A `ForeignNode` targeting `strand-builtin:Time.Now`, declaring `Time.Now` as its effect. Evaluates to the reference implementation's fixed replay timestamp under a context that grants the effect. |
| 17-builtin-compose-pure-and-effectful.json | run (under {Time.Now}) | Composes the pure builtin `Int.Add` with the effectful builtin `Time.Now`: `add(now(), 1)`. Demonstrates effect closure propagation through Application and that pure builtins can be called freely while effectful ones gate on capability. Evaluates to `replay-timestamp + 1`. |
| 18-match-int-literal-with-wildcard.json | run | A `Match` on `IntLit 0` with two cases: a literal pattern matching `0` (returns `100`) and a wildcard pattern (returns `200`). Scrutinee is `0`, so the first case wins. Evaluates to `100`. The smallest meaningful pattern-matching example: literal vs. wildcard. |
| 19-match-on-comparison-result.json | run | A `Match` on the `Bool` result of `Int.Lt(3, 5)`. Two cases pattern-match `BoolLit(true) → 1` and `BoolLit(false) → 0`. This is the canonical `if`-as-`match` idiom: branching on a comparison via two literal Bool patterns. Evaluates to `1`. |
| 20-match-variable-binding.json | run | A `Match` on `IntLit 7` with a variable pattern that binds the scrutinee to `n` for use inside the case body. The body computes `n + 10` via `Int.Add`. Demonstrates `VariablePattern` as a new binding occurrence — `VarRef.binder` points at the pattern's `NodeId`. Evaluates to `17`. |
| 21-fixpoint-factorial.json | run | The first recursive program: factorial via `Fixpoint` + `Match` + comparison/arithmetic builtins. `fact(0) = 1`; otherwise `fact(n) = n * fact(n-1)`. The body Lambda's first parameter is the recursive call slot, supplied at runtime by the `Fixpoint`. Applied to `5` → `120`. |
| 22-fixpoint-sum-to-n.json | run | Sum of 1..N via `Fixpoint` + `Match`. `sum(n) = 0` if `n <= 0`, else `n + sum(n-1)`. Pairs `Int.Le` for termination with `Int.Add` and `Int.Sub` for the inductive step. Applied to `10` → `55`. |
| 23-product-construct-and-access.json | run | Construct a `{x: 3, y: 4}` point via `ProductValue` and read field `y` with `ProductFieldGet`. The smallest end-to-end product example: closes the long-standing gap where product types could be declared but not instantiated. Evaluates to `4`. |
| 24-product-sum-fields-via-lambda.json | run | A function over the same `Point` type that reads both fields and sums them. Demonstrates that `ProductValue`s flow normally through `Application` boundaries and that field-access composes with arithmetic. Applied to `{x: 10, y: 32}` → `42`. |
| 25-option-some-unwrap.json | run | Option-like sum (`Some(Int)` / `None`). Construct `Some(42)`, `Match` with constructor patterns binding the payload; the `Some(n) → n` case returns `42`. |
| 26-option-none-default.json | run | Same Option type as 25; this time the constructed value is `None`, so the `Some(n)` pattern fails and the `None → -1` case wins. Demonstrates the no-payload constructor pattern. |
| 27-result-ok-or-err.json | run | Result-like sum (`Ok(Int)` / `Err(String)`). Construct `Ok(7)`, `Match` with two constructor patterns of different payload types. The `Ok(n) → n + 100` case wins; `Err(_) → -1` would handle the error path. Evaluates to `107`. |
| 28-safe-divide-success.json | run | The first program combining the full vocabulary: a `safe-divide` Lambda returns `Option<Int>` (`None` on zero divisor, `Some(n/d)` otherwise) via `Match` on the `Int.Eq` comparison. The caller pattern-matches the `Option` result. `divide(10, 2)` → `Some(5)` → unwraps to `5`. |
| 29-safe-divide-by-zero.json | run | Same `safe-divide` function as 28 but applied to `divide(10, 0)` → `None` → falls back to `-1`. Together with 28, demonstrates both code paths and the Option-returning idiom for fallible operations. |
| 30-string-concat.json | run | Minimal `String.Concat` builtin demo: `"Hello, " + "Strand!"` → `"Hello, Strand!"`. Smallest program with a non-Int / non-Bool result. |
| 31-recursive-list-head.json | run | First program over a recursive type. Defines `List = μ. Nil | Cons(head: Int, tail: List)` via `RecursiveType` + `RecursiveSelf`. Constructs `Cons(7, Nil)` and extracts `head` via `Match` with a `ConstructorPattern` binding the payload product, then `ProductFieldGet`. Result: `7`. |
| 32-recursive-list-sum.json | run | The capstone: sum the elements of a recursive linked list `[1, 2, 3]` via `Fixpoint` + `Match` over the recursive sum type. Demonstrates the full language vocabulary in a single program — recursive types, sum constructors, product field access, pattern matching with variable binding, fixed-point recursion, and foreign builtin arithmetic. Result: `6`. |
| 33-refined-network-connect.json | run (under `Network.Connect{host: "api.example.com", port: 443}`) | Q-031 step one: `Network.Connect(api.example.com, 443)` with an `Application.effectInstances` entry whose EffectDecl parameters are the host and port arguments. The runtime grants a Concrete refinement covering the same host and port. Demonstrates the smallest end-to-end positive refinement match. |
| 34-refined-wildcard-port.json | run (under `Network.Connect{host: "api.example.com", port: *}`) | Q-031 step two: same `Network.Connect` shape but the granted capability uses a `Wildcard` port slot. The call site supplies port 8080; the wildcard matches any port. Demonstrates per-slot wildcards as the "this host on any port" policy idiom. |
| 35-refined-logger-authorized-path.json | run (under `Filesystem.Write{path: "/var/log/app.log"}`) | Q-031 step three (positive confused-deputy path): a `logger` Lambda that writes to a caller-supplied path. The outer call passes `"/var/log/app.log"` — the path the granted capability authorizes — and the inner write succeeds. The negative case (caller passes `"/etc/passwd"` and the write is denied with `RefinementViolation`) is covered by `InterpreterTest`'s scenario 9 because it is a denial test, not a runnable corpus entry. |
| 36-handler-mock-time-now.json | run (under `{}`) | Q-030 scenario 1: smallest end-to-end Handler. The body calls `now()` (declares Time.Now); the Handler intercepts and returns a fixed `1700000000`. Runs under EMPTY capabilities — closure subtraction removes Time.Now from the body's effective requirements. The `now()` builtin is never invoked. |
| 37-handler-captures-outer-let.json | run (under `{}`) | Q-030 scenario 2: a Handler whose `handle` Lambda reads a value bound by an outer `Let`. Confirms the handler's lexical environment is captured at Handler-entry: the binding `t = 1700000000` is in scope when the handler is evaluated, and the handler returns it whenever Time.Now is intercepted. |
| 38-handler-nested-innermost-wins.json | run (under `{}`) | Q-030 scenario 3: two Handlers for the same `Time.Now` category nested inside each other. The body calls `now()`; the innermost Handler's `handle` returns `2`, the outer's returns `1`. The interpreter's `findLast` selects the innermost active handler — innermost wins, so the result is `2`. |
| 39-handler-itself-performs-effect.json | run (under `{Filesystem.Write}`) | Q-030 scenario 4: the Handler intercepts Time.Now; its `handle` Lambda itself performs `Filesystem.Write` to log the intercept. By the closure rule `closureOf(handler) = (closureOf(body) - {intercept}) ∪ closureOf(handle)`, the surrounding context needs Filesystem.Write but NOT Time.Now. Result is `0` — the reference Filesystem.Write builtin returns bytes written. |
| 40-handler-fires-through-fixpoint.json | run (under `{}`) | Q-030 scenario 6: a recursive function over `Fixpoint` that wraps every `now()` call in a freshly-installed Handler returning `1`. Regression test for handler threading through Fixpoint self-references: `f(3)` returns `1 + 1 + 1 + 1 = 4`, one handler firing at each recursive level `n = 3, 2, 1, 0`. |
| 41-toggle-machine.json | machine (3x unit events) | Q-032 step 1 scenario 1: smallest state machine. State is `Bool`; the single input stream carries `Unit` events. Transition: `\(s, _) -> (not s, {})`. No output streams. Drive with three unit events; state trajectory is `false → true → false → true`. The OutputBatch is the empty product `{}` (zero output streams). |
| 42-counter-machine.json | machine | Q-032 step 1 scenario 2: state is `Int`; event type is a sum `Increment | Decrement | Reset`. Transition uses `Match` on the event sum. No outputs. Demonstrates sum-typed events and `Match` inside a transition function — the same construct corpus programs 25–27 used at the expression level, now inside a state-machine transition. |
| 43-counter-with-overflow-output.json | machine | Q-032 step 1 scenario 3: counter with a single output stream. State `Int`, events `Increment | Decrement`. Transition emits `Some(state)` to the output stream when the new state exceeds threshold 5, otherwise `None`. First program with a non-empty OutputBatch (one slot, `output_0: Option<Int>`). Uses `Let` to share the new-state subexpression across the result product and the threshold check. |
| 44-request-response-echo.json | machine | Q-032 step 1 scenario 4: product-typed events. Event type is `{requestId: Int, payload: String}`; state is `Unit` (stateless). Output emits `{requestId, response: payload}` for every event via a `Some(_)`. Demonstrates `ProductFieldGet` inside a transition body and a product-typed output. |
| 45-bank-account-machine.json | machine | Q-032 step 1 capstone. State is `{balance: Int, transactionCount: Int}`; event sum `Deposit(Int) | Withdraw(Int) | Query`. Transition uses constructor patterns to destructure the event, `ProductFieldGet` to read the balance, `Int.Ge` for the overdraft check, and a nested `Match` on the comparison result. Output emits `Some(balance)` on Query, `Some(-1)` as an overdraft signal on a failed Withdraw, `None` on successful Deposit/Withdraw. Combines every feature added in Layer 5 (Match, products, sums, conditionals, field access, builtins) inside a single transition function. |
| 46-async-single-machine-counter.json | async group | Q-033 step 2 scenario 1: smallest end-to-end async demo. State `Int` starting at 0; each `Int` event accumulates into the running sum and emits the new state on the external output stream. Driven through `StateMachineRuntime.runGroup` rather than the synchronous fold; demonstrates that the per-machine coroutine actor consumes events from a `Channel<Value>` and writes outputs to another channel. `AsyncCorpusTest` also exercises the replay-determinism seam: the per-instance recorder captures every consumed event and re-feeding the list to `runMachine` against the same machine NodeId reproduces the per-step trace. |
| 47-async-multi-input-merge.json | async group | Q-033 step 2 scenario 2: smallest multi-input machine. Two external `Int` input streams (`inputA`, `inputB`) and one `String` output. The transition function's Event parameter is the synthesized `stream_0(Int) \| stream_1(Int)` InputEvent sum (user-declared as `inputEventT` and matched structurally by the verifier); the body `Match`es on the sum and emits `"A"` or `"B"` accordingly. State is an Int counter. Tests both the verifier's multi-input lift (Q-033's primary verifier change) and the runtime's per-stream FIFO with nondeterministic merge across streams (Q-009 default). Routed events file specifies the destination stream per event. |
| 48-async-supervisor-one-for-one.json | async group | Q-033 step 2 capstone: a three-machine group wired through internal streams. `workerA` reads from `extInputA` and emits the running sum to `chanA`; `workerB` does the same on `chanB`; the `supervisor` reads both internal streams via the synthesized two-case InputEvent sum and emits a running event-count on `extOutput`. Demonstrates inter-machine wiring (the internal channels are shared between producer and consumer actors), the supervisor pattern as a state machine (no new node category needed — supervisor is just a multi-input machine per ADR-007), and the Let-chain root pattern for packaging multiple machines in one program so `Hasher.finalize` keeps them all reachable. The CLI's `strand group` walks the canonical store for all StateMachine NodeIds; the test mirrors the same pattern. Real spawn/restart of children remains deferred to step 3 with the E-030/E-031 effects. |
| 49-async-tagged-output-list.json | async group | Q-033 step 2 scenario 3: the tagged-list output shape. One `Int` input, two `Int` outputs (`outputA`, `outputB`). For each event, the transition emits to BOTH output streams via the recursive list `Cons(output_0(state), Cons(output_1(doubled), Nil))` over the synthesized `TaggedOutput` sum (where `TaggedOutput = output_0(Int) \| output_1(Int)`). Exercises (a) the verifier's tagged-list expected-result shape (the alternative to OutputBatch for transitions that emit zero, one, or many events per step), (b) `MachineActor.dispatchTaggedList` walking the list at runtime, and (c) per-output-stream FIFO when one transition fires multiple emissions. State is the running sum; doubled = `state * 2`. |
| 50-positive-int-schema-pass.json | schema-check | Q-035 step 1 scenario 1: smallest end-to-end Schema demo. A `PositiveInt` Schema with valueType `Int` and a single `x_positive` invariant `\x -> x > 0`. The program applies an `identityOfPositiveInt: PositiveInt -> PositiveInt` Lambda to `IntLit(5)`. Verifier accepts; SchemaChecker statically evaluates the invariant and accepts (5 > 0). |
| 51-positive-int-schema-fail.json | schema-check | Q-035 step 1 scenario 2: same `PositiveInt` Schema as 50, applied to `IntLit(-3)`. Verifier accepts at the type level; SchemaChecker statically evaluates `-3 > 0 = false` and produces a `SchemaInvariantViolation` pointing at the `x_positive` invariant. |
| 52-non-empty-list-schema-pass.json | schema-check | Q-035 step 1 scenario 3: Schema over a recursive type. `NonEmptyList` wraps the recursive list type `μ. Nil \| Cons(head: Int, tail: <self>)` with an invariant `non_empty` that pattern-matches on the list — `Cons(_) -> true`, `Nil -> false`. Applied to `Cons(1, Nil)`. SchemaChecker recursively evaluates the SumValue (the static-value evaluator handles SumValue + ProductValue + literal payloads) and runs the Match-based invariant body, which returns true. |
| 53-non-empty-list-schema-fail.json | schema-check | Q-035 step 1 scenario 4: same `NonEmptyList` Schema as 52, applied to `Nil`. SchemaChecker evaluates the Match on Nil → false, produces a `SchemaInvariantViolation` on `non_empty`. Demonstrates Schema-over-recursive-type rejection. |
| 54-json-value-primitives.json | schema-check | First program in the blessed JSON library (Q-026). `JsonValue` is a flat sum over `JsonNull \| JsonBool(Bool) \| JsonNumber(Int) \| JsonString(String)`; the `JsonValue` Schema wraps it with no invariants. The program constructs `JsonNumber(42)` and applies an `identityOfJsonValue: JsonValue -> JsonValue` Lambda to it. Smallest demonstration that the schema mechanism plumbs through for a real (non-synthetic) value type. Arrays and nested objects are deferred — see [`proposals/implemented/json-blessed-library.md`](../../../../proposals/implemented/json-blessed-library.md) for the design choice on the nested-μ limitation. |
| 55-json-object-unique-keys.json | schema-check | The `UniqueKeyJsonObject` Schema demonstration. `JsonObject` is a recursive list `μ. Cons({head: JsonEntry, tail: <self>}) \| Nil` over `JsonEntry = {key: String, value: JsonValue}`. The Schema wraps `JsonObject` with a `unique_keys` invariant whose body is a `Fixpoint`-recursive Lambda that walks the entries list and, for each Cons, checks (via a nested `Fixpoint` `contains` over the tail) that the head's key does not appear later. Built from `String.Eq`, `Bool.And`, `Bool.Or`, `Bool.Not` builtins (which Q-033 made the runtime aware of and Q-026 now uses for the first time). The program constructs a three-entry object `{name: "Strand", version: 1, enabled: true}` and applies it through `identityOfUniqueObject: UniqueKeyJsonObject -> UniqueKeyJsonObject`. SchemaChecker statically evaluates the invariant (since the object literal is fully static) and accepts. |
| 56-json-object-duplicate-keys-fail.json | schema-check | Same `UniqueKeyJsonObject` Schema and same identity function as 55, but the object literal has two entries both keyed `"name"`. SchemaChecker statically evaluates the `unique_keys` invariant — the `contains("name", tail)` call returns true at the head entry, `Bool.Not(true) = false`, the And short-circuits, the overall invariant returns false. Produces `SchemaInvariantViolation(invariant=uniqueKeysInvariant)` pointing at the offending object. Demonstrates SchemaChecker's ability to evaluate non-trivial `Fixpoint` + `Match` invariant bodies via `Interpreter.applyCallable`. |

State-machine programs ship paired with `<id>-...events.json` event-list files for the `strand machine` CLI subcommand. The event-list JSON schema is documented in `runtime/EventCodec.kt`.

The `layer-a/` subdirectory holds Layer A authoring-projection forms (Q-034 step 1) for a representative subset of the corpus — currently 14 programs covering all major features: literals/types/binding (01-04), effects + foreign (12, 15), match patterns (18), fixpoint + recursive types (21, 31), product/sum values (23, 25), handlers (36), state machines (41), schemas (50). Each `<basename>.layer-a` is a compact-text projection of the corresponding `<basename>.json`; the round-trip property (Layer A → `Authoring.compileToDagJson` → canonical store hashes equal to the canonical JSON's canonical store) is asserted in `LayerARoundTripTest`. **Measured byte compression: 3.39× total across the 14-program subset** (per-program range 2.82×–4.40×), within the proposal's projection-only estimate. The canonical JSON files remain the authoritative form; Layer A is an additional projection of the same graph. Adding a new pair requires (a) the Layer A file, (b) a new entry in `LayerARoundTripTest.pairs`. The Layer A grammar (42 codes in `LayerAGrammar.codes`) covers all currently-implemented node categories except N-030 Name and N-031 Provenance (metadata-only; no current usage); extending coverage to those two is a small grammar addition when a corpus program needs it.

Two TypeParameter nodes are the same type variable when they are the same
node; consequently programs 07 and 11 share a single `T_a` node between the
ForallType that classifies the higher-rank parameter and the TypeAbstraction
that produces the polymorphic argument. Layer 1 compares Forall types by
structural equality on their NodeId-keyed bindings; Layer 2's canonical
encoding will perform the alpha-equivalence normalization that lifts this
sharing requirement.
